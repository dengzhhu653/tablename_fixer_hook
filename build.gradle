plugins {
  id 'com.palantir.git-version' version '0.12.2'
  id 'eclipse'
  id 'application'
}

version = gitVersion()

repositories { mavenCentral() }

dependencies{
  runtime 'org.apache.hive:hive-exec:3.1.2'
}
